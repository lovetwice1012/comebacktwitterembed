# Retain core availability semantics even when a future release replaces the
# base unit file. Manual `systemctl stop` remains a deliberate stop.
[Unit]
StartLimitIntervalSec=0

[Service]
Restart=always
