# ダッシュボード再調査と本番検証

2026-09-07 JST。更新後の本番ソース、レポートworker、MySQLを再確認し、詳細分析・サーバー分析・マーケ分析を実際に生成した。

## 再現した原因

レポートworkerがSQLへ付ける所有確認コメントに`/*cbte-report-query:<UUID>*/`というコロンを含めていた。PrismaがUUIDの一部を名前付きパラメーターとして扱い、通常の`?`パラメーターと混在したSQLとして拒否していた。これはSQLの実行時間以前の失敗だった。

また、更新後の詳細分析には、同じファセット表を全期間で何度も走査する処理が残っていた。数値メトリクスの最新観測、プロバイダーのマーケ軸、スキーマ観測数が重複していた。

## 反映した修正

- レポート識別コメントをコロンなしに変更し、`?`パラメーターと衝突しないようにした。
- 詳細分析、サーバー分析、マーケ分析は起動時・閲覧時・経過時間だけでは生成しない。ボタンの明示的なリクエストだけが生成を開始する。
- 前回成功したレポートは保持し、失敗時に空の結果へ置き換えない。
- ファセット、マーケ軸、関心、寿命、URL再利用、設定効果、信頼性の集計を、候補を先に絞ってから厳密なユーザー・サーバー数を求める形へ変更した。
- 数値メトリクスは、対象プロバイダーの登録済みキーを先に絞り、スキーマ表示だけが必要な観測数は単純な集計へ分離した。
- レポートworkerのSQL上限を120秒から300秒へ変更した。全体の生成予算は600秒のまま。明示的な生成のための上限であり、画面を開いただけではSQLを発行しない。

## 本番実測

1時間範囲のボタン相当生成:

- 詳細分析: 19.9秒、24セクション、成功
- サーバー分析: 7.4秒、26セクション、成功
- マーケ分析: 7.8秒、27セクション、成功

標準7日範囲の完全生成:

- 詳細分析: 388.8秒、成功
- サーバー分析: 280.8秒、成功
- マーケ分析: 334.6秒、成功

標準範囲で最長だったSQLは、詳細分析の数値メトリクス集計273.9秒、マーケ分析の数値メトリクス集計160.1秒、サーバー分析の数値メトリクス集計156.1秒だった。いずれも300秒のSQL上限内で完了した。

本番SQLの実行中も、Botは稼働を継続した。最終確認でBotサービスはactive、NRestarts=0。公開URLとローカルURLはともにHTTP 200。MySQLの利用可能メモリは約7.8GiB、swap使用量は約0.9GiBだった。

## 検証

- 現行ブランチの全テスト: 574件中573件成功、1件skip、失敗0件。
- 手動生成・詳細SQLを含むデプロイブランチの全テスト: 586件中585件成功、1件skip、失敗0件。
- dashboardのtypecheck、lint成功。
- 本番の1時間範囲3種類、標準7日範囲3種類をworkerと同じ`buildReport`経路で成功確認。

本番のレポートworkerは`/opt/cbte-admin/runtimes/2244f3bdf136d6a8122c36a895fdec50ea5a6147`へ切り替え、SQL上限設定を`/etc/cbte-admin/reports.env`で300秒へ変更した。Bot本体の再起動は行っていない。


## SQL集計統合後の再計測

`90a072e` で数値メトリクスSQLを追加最適化した。`ranked` CTEから最新値、イベント数、ユーザー数、サーバー数を同時に集計し、従来の観測集合の再集計をなくした。最新行の順位条件と集計対象は変更していない。

同じ本番worker経路で再計測した結果:

- 1時間範囲: 詳細分析 3.1秒、サーバー分析 6.7秒、マーケ分析 6.1秒
- 7日範囲: 詳細分析 307.5秒、サーバー分析 333.3秒、マーケ分析 309.7秒
- 数値メトリクスSQL: 詳細分析 195.8秒、サーバー分析 131.1秒、マーケ分析 123.7秒

旧計測との比較では、数値メトリクスSQLは詳細分析で約28%、サーバー分析で約16%、マーケ分析で約23%短縮した。サーバー分析全体は時系列集計が82秒前後に伸びたため、レポート全体では旧値を下回らなかった。SQL統合による数値メトリクス部分の改善と、他SQLの時間変動は分けて評価する必要がある。


## 最新観測ロールアップの本番導入

`bot_provider_metric_observation_hourly` を追加し、14日分を再構築した。対象は1,661,890行、305個の非空時間バケットで、標準7日範囲を含む。ロールアップは1時間内の最新観測だけを保持し、期間境界の時間帯とイベント・ユーザー・サーバー数は生テーブルで正確に補完する。

ロールアップ参照後の本番実測:

- 詳細分析: 最新値SQL 29.5秒、正確な件数SQL 95.9秒、全体 257.2秒
- サーバー分析: 最新値SQL 20.2秒、正確な件数SQL 61.3秒、全体 224.9秒
- マーケ分析: 最新値SQL 19.3秒、正確な件数SQL 61.2秒、全体 261.9秒

新規データへの追従は、直近3時間を15分間隔で再構築するsystemd timerでも維持する。timerの初回実行は成功し、レポートworkerはロールアップ参照設定を有効化した。


## 管理daemonのBot障害判定修正

管理daemonは、systemd unitがactiveのままguardianだけ残り、Bot Node子プロセスが不在になるケースを検知しても、`bot.workload.unverified` incidentの記録と診断起動だけで停止していた。自動再起動関数は検証済みheartbeatとの一致を必須としていたため、実Botが不在のケースでは到達できなかった。さらに新規policyの`autoRestartHungBot`既定値がfalseだった。

`69132cd` で、activeなunitのInvocationIDを再確認し、3回以上のworkload未検証、heartbeatのstale/unobserved、失敗したlocal health、直近の独立DB疎通成功、再起動回数制限を満たす場合に、同じunit InvocationIDへ一度だけ`service.restart`をenqueueする経路を追加した。maintenance/停止意図、systemd job競合、restart cooldown/daily limitは従来どおり優先する。新規インストールのpolicyでは`autoRestartHungBot`をtrueにした。既存policyの明示的なfalseは上書きしない。


## 2026-09-08 管理daemonの未検証Bot復旧調査

公開URLの`/api/health`とルートはHTTP 530、本文はCloudflare error code 1033だった。SSHのCloudflare tunnelも`bad handshake`後に接続resetとなり、本番ホストへ到達できなかった。この時点ではBotの実プロセス状態と既存policyを本番で確定できない。

ソース上は、guardian unitがactiveのままBot Node子プロセスだけ消えた場合、monitorは`bot.workload.unverified`を記録して診断を起動するが、再起動関数は検証済みheartbeatを必須としていたため未検証経路から到達できなかった。また、新規policyの`autoRestartHungBot`既定値がfalseで、公開経路の判定もlocal healthが成功した場合だけ実行していた。

`69132cd`、`8a8e792`、`aa01430`で、active unitのInvocationID、3回以上の未検証、stale/unobserved heartbeat、独立DB疎通、再起動制限を確認して同じunitへ一度だけrestartをenqueueする経路を追加した。新規policyでは自動復旧を有効化し、公開経路の失敗はlocal healthの結果に関係なく記録する。Cloudflare tunnel復旧後にGoのコンパイル・テスト、本番policy反映、管理daemon再起動、実プロセス証拠の確認が必要。
